! dim文は配列を宣言するために用いられる。
DIM A(10)          ! A(1),…,A(10) の10個の添字つき変数を用意する。
DIM B(0 TO 10)     ! B(0),…,B(10) の11個の添字つき変数を用意する。
DIM C(4,4)         ! C(1,1),C(1,2),C(1,3),…,C(4,4)の16個の添字つき変数を用意する。
FOR i=1  TO 10
    LET A(i)=i*2
NEXT i
FOR i=1 TO 10
    PRINT A(i)
NEXT i
END

