! Q. sin(pi)は0にならないのですか?
! A. 組込み関数piは無理数πを有限桁の十進数で近似した値です。
!    ですから，sin関数の計算が正しければ，sin(pi)は決して0になりません。
! 　 実際、SIN(PI)＝sin(π－pi)≒π－piとなります。
PRINT USING "#.#####################################": PI
PRINT USING "#.#####################################": SIN(PI)
END
